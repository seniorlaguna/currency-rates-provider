on: push

jobs:
  main:
    runs-on: ubuntu-latest
    name: Publish currencies and rates
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Fetch and prepare
        uses: ./