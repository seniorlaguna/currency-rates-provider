on:
  push:
  schedule:
    - cron: '*/27 3-21 * * *'

jobs:
  main:
    runs-on: ubuntu-latest
    name: Publish rates
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Fetch and prepare
        uses: ./
        env:
          KEY1: ${{ secrets.API_KEY_1 }}
      - name: Add & Commit
        uses: EndBug/add-and-commit@v9.1.1
